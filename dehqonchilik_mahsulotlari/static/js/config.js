// API Configuration
const API_URL = '/api/v1';  // Relative URL when served by Django

// App Configuration
const APP_CONFIG = {
    apiUrl: API_URL,
    defaultPageSize: 20,
    currency: 'UZS',
    phoneRegex: /^\+998[0-9]{9}$/,
};
